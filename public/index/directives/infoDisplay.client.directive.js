angular.module('index').directive('infoDisplay', ['$scope', function($scope){
   restrict: 'E',
   controller: ['$scope', function($scope){
      
      for(var i in $scope.messages){

      }
   }]
      }
}])